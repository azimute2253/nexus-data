# Story 7.3 — Score Normalization Logic

**Phase:** 7 — Scoring System
**Status:** Ready for Review
**Effort:** S
**Dependencies:** 7.2 (scores must be saved for normalization), 4.3 (L3 consumes normalized scores)

## Description

As a developer,
I want the score normalization function that converts raw scores into ideal % per asset within a group,
so that the L3 rebalancing distribution uses correct proportions based on questionnaire results.

Score normalization handles negative scores by shifting the minimum to zero before computing proportions. This ensures all assets get non-negative allocation weights.

[Source: PRD §3 Module 4, Feature F-006 AC5]

## Acceptance Criteria

- [x] Given assets in a group with scores [8, 5, 3, -2], when normalized, then each gets a proportional % of the group's allocation
- [x] Given the normalization, when a negative score exists, then the minimum is shifted to zero: [8, 5, 3, -2] → [10, 7, 5, 0] → percentages: [45.5%, 31.8%, 22.7%, 0%]
- [x] Given all scores in a group are identical (e.g., all 5), when normalized, then each asset gets equal % (1/N)
- [x] Given a single asset in a group, when normalized, then it gets 100%
- [x] Given the `normalizeScores()` function in `src/lib/nexus/rebalance.ts`, when called, then it is a pure function with no side effects, no database calls, and no imports beyond TypeScript standard library
- [x] Error: Given all assets in a group have score 0 (after shift), when normalized, then equal distribution (1/N) is used as fallback — e.g., 3 assets each get 33.33%
- [x] Error: Given an empty scores array [], when `normalizeScores([])` is called, then it returns an empty array [] without throwing

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T7.3.1 | `normalizeScores([8, 5, 3, -2])` | Shift by +2 → [10, 7, 5, 0], sum=22 → [45.45%, 31.82%, 22.73%, 0%] | Sum = 100% (tolerance < 0.01%), each value matches |
| T7.3.2 | `normalizeScores([5, 5, 5])` | Equal: [33.33%, 33.33%, 33.33%] | Each = 1/3, sum = 100% |
| T7.3.3 | `normalizeScores([7])` | [100%] | Single element returns exactly 100 |
| T7.3.4 | `normalizeScores([0, 0, 0])` | Equal fallback: [33.33%, 33.33%, 33.33%] | Each = 1/N, sum = 100% |
| T7.3.5 | `normalizeScores([-10, -5, -3])` | Shift by +10 → [0, 5, 7], sum=12 → [0%, 41.67%, 58.33%] | Proportional after shift, sum = 100% |
| T7.3.6 | `normalizeScores([])` | Empty array [] | Returns [], no error thrown |

## Dev Notes

- **Primary file:** `src/lib/nexus/rebalance.ts` — `normalizeScores()` function
- **Constraints:** Must support negative scores — shift minimum to zero (ADR-004 Obligation 4)
- **Constraints:** Pure function, part of isomorphic rebalance module (ADR-004)
- **Constraints:** This function is consumed by `distributeL3()` in Story 4.3
- [Source: PRD §3 Module 4, Feature F-006 AC5]
- [Ref: ADR-004 (Score normalization specification — Obligation 4)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/lib/nexus/rebalance.ts` | Modify | Add `normalizeScores()` function |
| `tests/unit/normalize-scores.test.ts` | Create | Unit tests for score normalization |

## Dev Agent Record

**Agent Model Used:** Claude Opus 4.6

### Change Log

| File | Action | Details |
|------|--------|---------|
| `src/lib/nexus/rebalance.ts` | Modified | Added `normalizeScores()` pure function (lines 153–183) |
| `tests/unit/normalize-scores.test.ts` | Created | 15 unit tests covering all T7.3.x cases + edge cases |
| `docs/stories/7.3.story.md` | Modified | Checked ACs, updated status, added Dev Record |

### Completion Notes

- All 7 acceptance criteria satisfied and verified by tests
- 15 unit tests: T7.3.1–T7.3.6 + 4 additional edge cases (equal-after-shift, determinism, immutability, two-element negative)
- Full regression: 122/122 tests pass (6 test files)
- TypeScript strict mode: `npx tsc --noEmit` — clean
- Pure function: no imports, no side effects, no DB calls
