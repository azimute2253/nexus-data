# Story 4.2 — L2 Distribution (Group-Level Rebalancing)

**Phase:** 4 — Rebalancing Algorithm
**Status:** Ready for Review
**Effort:** S
**Dependencies:** 4.1 (L1 must produce per-type amounts for L2 input), ADR-004 (Rebalancing Algorithm Architecture)

## Description

As a user,
I want the L2 cascade to distribute each type's allocated amount among its groups based on group target percentages,
so that within each asset class the money flows to the correct subgroups.

L2 takes the output of L1 (amount per type) and distributes within each type among its groups according to each group's `target_pct`. The sum of group targets within a type must equal 100%.

[Source: PRD §3 Module 2, Feature F-002 AC4]

## Acceptance Criteria

- [ ] Given L1 allocates R$4,428 to "Acoes BR" with 4 groups (60%, 20%, 10%, 10%), when `distributeL2()` runs, then groups receive R$2,656.80, R$885.60, R$442.80, R$442.80 respectively
- [ ] Given a type with a single group at 100%, when L2 distributes, then the full type amount goes to that group
- [ ] Given a type that received R$0 from L1, when L2 processes it, then all its groups receive R$0
- [ ] Given the function, when inspected, then it is a pure function with no side effects
- [ ] Error: Given the sum of group targets within a type does not equal 100%, when `distributeL2()` is called, then it throws a descriptive validation error

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T4.2.1 | R$4,428 to type with 4 groups (60/20/10/10) | Groups: 2656.80, 885.60, 442.80, 442.80 | Per-group amounts within R$0.01 |
| T4.2.2 | R$0 to any type | All groups = R$0 | `every(group => group === 0)` |
| T4.2.3 | Type with 1 group at 100% | Group gets full amount | `group === typeAmount` |
| T4.2.4 | Group targets don't sum to 100% | Validation error thrown | Error with descriptive message |

## Dev Notes

- **Primary file:** `src/lib/nexus/rebalance.ts` — `distributeL2()` function
- **Constraints:** Pure function, deterministic (ADR-004)
- **Constraints:** Group target validation: sum must equal 100% within each type (PRD F-004 AC4)
- [Source: PRD §3 Module 2, Feature F-002 AC4]
- [Ref: ADR-004 (Rebalancing Algorithm Architecture)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/lib/nexus/rebalance.ts` | Modify | Add `distributeL2()` function |
| `src/lib/nexus/types.ts` | Modify | Add `L2GroupInput` and `L2Result` interfaces |
| `tests/unit/rebalance-l2.test.ts` | Create | Unit tests for L2 distribution (15 tests) |

## Dev Agent Record

### Agent Model Used
claude-opus-4-6

### Tasks
- [x] Add `L2GroupInput` and `L2Result` types to `src/lib/nexus/types.ts`
- [x] Implement `distributeL2()` in `src/lib/nexus/rebalance.ts`
- [x] Create `tests/unit/rebalance-l2.test.ts` with 15 tests (T4.2.1–T4.2.4 + edge cases)
- [x] Run `npx tsc --noEmit` — Type check passes (zero errors)
- [x] Run `npx vitest run` — All 122 tests pass (6 test files, zero regressions)

### Debug Log
*(No issues encountered)*

### Completion Notes
- **Pure function:** `distributeL2()` takes L1 results + group definitions, returns per-group allocations. No side effects, deterministic.
- **Formula:** `allocated = l1.allocated * group.target_pct` — simple cascade of type-level amounts.
- **Validation:** Group `target_pct` values within each type must sum to 1.0 (±0.001 tolerance for floating-point). Throws descriptive error otherwise.
- **Edge case — no groups for a type:** Types with no matching groups produce no L2 results (silent skip).
- **L2 types:** `L2GroupInput` (input) and `L2Result` (output) added to `types.ts`, consistent with L1 naming conventions.

### Change Log
| Date | Change |
|------|--------|
| 2026-03-22 | Story 4.2 implemented — `distributeL2()` + types + 15 unit tests |

## QA Results

**Reviewer:** Quinn (@qa) — claude-opus-4-6
**Date:** 2026-03-22
**Verdict:** PASS (12/12)

| Category | Score | Details |
|----------|-------|---------|
| A. Implementation | 6/6 | L1→L2 cascade correct, formula matches PRD, validation enforced, edge cases handled |
| B. Tests | 4/4 | Multi-type, single-type, edge cases, L1 integration — all covered (15 tests, 0 failures) |
| C. Code Quality | 2/2 | Full type safety (tsc clean), block comments + JSDoc on types |

**AC Traceability:** All 5 acceptance criteria covered by tests (T4.2.1–T4.2.4 + determinism).
**Issues:** None.
