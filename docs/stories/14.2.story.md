# Story 14.2 — Dual Weight UI (Mode Switch + Manual Input + Questionnaire Integration)

**Epic:** 14 — Dual Weight System
**Status:** Draft
**Effort:** M (3 points)
**Dependencies:** 14.1 (schema + data layer), 7.2 (existing ScoringModal component)
**PRD Feature:** F-028

## Description

As a user,
I want to see a weight mode switch on each asset (Manual / Questionário) with the appropriate input for each mode,
so that I can choose how to assign weight to each asset — either by entering a manual note or by answering the questionnaire.

When "Nota manual" is selected, a numeric input (-10 to +11) is shown. When "Questionário" is selected, the existing ScoringModal (from V1 Story 7.2) is shown. The current calculated weight is always displayed regardless of mode.

[Source: PRD-v2 §3 Module 15, Feature F-028; Creative V2 §Sistema de Peso por Ativo]

## Acceptance Criteria

- [ ] Given an asset detail/edit panel, when rendered, then a mode switch is visible with two options: "Nota manual" and "Questionário" (radio buttons or toggle)
- [ ] Given `weight_mode = 'manual'`, when the panel renders, then a numeric input for the manual weight (-10 to +11) is shown and the questionnaire section is hidden
- [ ] Given `weight_mode = 'questionnaire'`, when the panel renders, then the ScoringModal (existing V1 component) is accessible and the manual input is hidden
- [ ] Given the manual input, when a value outside -10 to +11 is entered, then a validation error is shown
- [ ] Given the panel, when the mode is switched, then the UI immediately updates to show the correct input for the new mode (no save required to see the change)
- [ ] Given the panel, when "Salvar" is clicked, then both `weight_mode` and the relevant weight value are persisted to the database
- [ ] Given any mode, when the panel is displayed, then the current calculated weight (normalized percentage within the group) is shown as a read-only indicator
- [ ] Given a mode switch, when saved, then the previous mode's data is preserved in the DB (manual_weight is not cleared when switching to questionnaire, and vice versa)

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T14.2.1 | Render panel for asset with weight_mode='manual' | Manual input visible, questionnaire hidden | DOM check |
| T14.2.2 | Render panel for asset with weight_mode='questionnaire' | Questionnaire accessible, manual hidden | DOM check |
| T14.2.3 | Switch from manual to questionnaire | UI updates immediately | DOM reactivity |
| T14.2.4 | Enter manual_weight = 12 | Validation error | Error message visible |
| T14.2.5 | Enter manual_weight = 7, save | Persisted in DB | GET returns weight_mode='manual', manual_weight=7 |
| T14.2.6 | Switch mode, save, switch back | Previous value preserved | manual_weight still has old value |
| T14.2.7 | Panel shows current weight percentage | Read-only indicator visible | Percentage displayed |

## Dev Notes

- **Primary file:** `src/components/nexus/DualWeightPanel.tsx`
- **Constraints:** Reuse existing `ScoringModal.tsx` from V1 (Story 7.2) for questionnaire mode — do NOT recreate
- **Constraints:** Mode switch is UI-only until "Salvar" is clicked (optimistic UI with rollback on error)
- **Constraints:** Weight display: show both the raw score and the normalized percentage within the group
- **Constraints:** Scale labels may be helpful (-10 = "Muito ruim", +11 = "Excelente") per Creative V2
- [Source: PRD-v2 F-028 AC3-AC5; Creative V2 §Sistema de Peso por Ativo (wireframes)]
- [Ref: ADR-015 (dual weight mode), Story 7.2 (ScoringModal)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/components/nexus/DualWeightPanel.tsx` | Create | Weight mode switch + manual input + questionnaire integration |
| `tests/unit/dual-weight-panel.test.tsx` | Create | Component tests for mode switch, validation, persistence |

## Dev Agent Record
*(Filled during implementation)*
