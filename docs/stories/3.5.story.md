# Story 3.5 — Manual Price Refresh Button

**Phase:** 3 — Price Engine
**Status:** Ready for Review
**Effort:** S
**Dependencies:** 3.4 (cron Edge Function must exist), 5.1 (dashboard page to host the button)

## Description

As a user,
I want to click an "Atualizar precos" button that forces an immediate price refresh,
so that I can get fresh prices on-demand before running a rebalancing calculation.

The button triggers the same Edge Function as the cron, but with a 1-minute debounce to prevent rate limit abuse.

[Source: PRD §3 Module 5, Feature F-007 AC5]

## Acceptance Criteria

- [ ] Given the dashboard page, when rendered, then an "Atualizar precos" button is visible
- [ ] Given the button is clicked, when the Edge Function completes, then all prices in `price_cache` are refreshed with new `fetched_at` timestamps
- [ ] Given the button was clicked less than 1 minute ago, when clicked again, then the request is debounced (no duplicate Edge Function invocation)
- [ ] Given the refresh is in progress, when the button is visible, then it displays a spinner icon and the button is disabled (non-clickable)
- [ ] Given prices are refreshed, when the dashboard re-renders, then updated prices and timestamps are displayed
- [ ] Error: Given the Edge Function fails, when the button handler catches the error, then an error message is shown to the user

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T3.5.1 | Click "Atualizar precos" | `price_cache` rows updated | `fetched_at` > timestamp before click |
| T3.5.2 | Click twice within 1 minute | Only 1 Edge Function invocation | `price_fetch_log` shows 1 entry |
| T3.5.3 | Click with API failure | Error toast shown | UI shows error message |
| T3.5.4 | Click while refresh in progress | Button disabled with spinner | `button.disabled === true && spinner visible` |

## Dev Notes

- **Primary file:** `src/components/nexus/PriceRefreshButton.tsx`
- **Constraints:** Debounce max 1 manual refresh per minute (ADR-005 Obligation 4)
- **Constraints:** Must call the same Edge Function as cron (ADR-005)
- **Constraints:** Hydration: `client:idle` — low priority (ADR-006 Hydration Strategy)
- [Source: PRD §3 Module 5, Feature F-007 AC5]
- [Ref: ADR-005 (Manual refresh — Obligation 4), ADR-006 (hydration directive)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/components/nexus/PriceRefreshButton.tsx` | Create | React island for manual price refresh with debounce |
| `src/lib/dashboard/refresh.ts` | Create | Shared refresh logic (cooldown, debounce guard, log, trigger) |
| `src/lib/nexus/types.ts` | Modify | Add PriceRefreshLog, PriceRefreshTrigger types |
| `tests/unit/price-refresh.test.ts` | Create | 18 unit tests for refresh + debounce logic |

## Dev Agent Record

**Agent Model Used:** Claude Opus 4.6 (@dev)
**Branch:** master

### Tasks
- [x] Create `PriceRefreshButton.tsx` — React component with spinner, disabled state, 1-min debounce, error display
- [x] Create `refresh.ts` — `createManualRefreshGuard()` with `canRefresh()`, `markRefreshed()`, `getTimeUntilNextMs()`
- [x] Create `triggerRefresh()` — calls refresh-prices Edge Function with auth header
- [x] Add `PriceRefreshLog` and `PriceRefreshTrigger` types to `types.ts`
- [x] Write 18 unit tests covering debounce guard, cooldown logic, and log insertion
- [x] Run `npx vitest run` — All 559 tests pass (29 test files)
- [x] Run `npx tsc --noEmit` — Type check passes

### Debug Log
- Fixed debounce guard initial state: `lastManualRefresh` initialized to `-MANUAL_DEBOUNCE_MS` to allow immediate first refresh

### Completion Notes
- Button renders "Atualizar precos" with refresh icon, transitions to spinner during loading
- Debounce guard is client-side in-memory (1-minute cooldown per AC3)
- Countdown timer shows remaining seconds when debounced
- Error messages displayed below button with `role="alert"` for accessibility
- Component designed for Astro `client:idle` hydration (ADR-006)

### Change Log
| Date | Change |
|------|--------|
| 2026-03-22 | Created PriceRefreshButton.tsx and shared refresh logic |
