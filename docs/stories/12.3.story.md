# Story 12.3 — Onboarding Flow (First Wallet)

**Epic:** 12 — Wallet UX
**Status:** Ready for Review
**Effort:** M (3 points)
**Dependencies:** 11.3 (wallet data access layer), 13.1 (tab navigation must exist for post-onboarding redirect)
**PRD Feature:** F-025

## Description

As a new user,
I want to be guided through creating my first wallet when I access Nexus Data for the first time,
so that I have a clear starting point and don't see an empty, confusing interface.

The onboarding is detected server-side (Astro frontmatter queries wallets) and renders conditionally on the same URL. After creating the first wallet, the user sees an empty Dashboard with guidance to start in the Ativos tab.

[Source: PRD-v2 §3 Module 12, Feature F-025; ADR-016 (Onboarding Flow)]

## Acceptance Criteria

- [x] Given a user with 0 wallets, when they access the Nexus Data page, then the onboarding screen renders instead of the Dashboard (server-side detection, no flash of wrong content)
- [x] Given the onboarding screen, when displayed, then it shows: welcome message ("Bem-vindo ao Nexus Data"), explanation text, and a wallet name input field with a "Criar minha carteira" button
- [x] Given the onboarding form, when a valid name is submitted, then a wallet is created, the `wallet_id` is saved to `localStorage`, and the page reloads to show the Dashboard
- [x] Given the post-onboarding Dashboard, when loaded with an empty wallet, then it shows an empty state: "Sua carteira está vazia. Comece adicionando suas classes de ativo na aba Ativos." with a link/button to navigate to the Ativos tab
- [x] Given a returning user with wallets, when they access Nexus Data, then the onboarding screen does NOT appear (goes straight to Dashboard)
- [x] Given the onboarding detection, when the Supabase query fails (service error), then a generic error page renders (NOT the onboarding screen)
- [x] Given the onboarding on mobile, when displayed, then it is fully responsive with no horizontal scroll

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T12.3.1 | User with 0 wallets accesses Nexus Data | Onboarding screen rendered | Welcome message visible |
| T12.3.2 | User with 1+ wallets accesses Nexus Data | Dashboard rendered | No onboarding elements |
| T12.3.3 | Submit "Minha Carteira" on onboarding | Wallet created, page reloads to Dashboard | Wallet exists in DB |
| T12.3.4 | Empty Dashboard after onboarding | Empty state message + link to Ativos | Message + link visible |
| T12.3.5 | Submit empty name | Validation error | Error message shown |
| T12.3.6 | Supabase query fails | Error page (not onboarding) | Error message visible |

## Dev Notes

- **Primary files:**
  - `src/components/nexus/OnboardingScreen.tsx` — Onboarding UI component
  - Astro page in azimute-blog (integration point for server-side detection)
- **Constraints:** Server-side detection in Astro frontmatter (ADR-016 D1) — query wallets, render conditionally
- **Constraints:** Same URL — no separate `/onboarding` route (ADR-016 D2)
- **Constraints:** After wallet creation: `window.location.reload()` to re-trigger server-side guard (ADR-016 D4)
- **Constraints:** Save wallet_id to localStorage immediately after creation (ADR-012)
- [Source: PRD-v2 F-025; Creative V2 §Tela de Onboarding]
- [Ref: ADR-016 (onboarding flow), ADR-012 (active wallet persistence), ADR-013 (tab routing)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/components/nexus/OnboardingScreen.tsx` | Create | Onboarding welcome + wallet creation form |
| `src/components/nexus/EmptyDashboard.tsx` | Create | Empty state for Dashboard post-onboarding |
| `tests/unit/onboarding.test.tsx` | Create | Onboarding component tests (22 tests) |
| `src/index.ts` | Modified | Added exports for OnboardingScreen + EmptyDashboard |

## Dev Agent Record

**Agent Model Used:** claude-opus-4-6
**Date:** 2026-03-24

### Completion Notes
- OnboardingScreen: React form component with wallet name input, validation (empty/whitespace), createWallet call, localStorage persistence, and page reload after success
- EmptyDashboard: Empty state component with message text and "Ir para Ativos" button (supports both callback and URL-based navigation via ADR-013)
- Server-side detection is handled at the Astro frontmatter level (in azimute-blog host) — components are conditionally rendered based on `hasWallets` boolean
- All Tailwind CSS inline styles, mobile-first responsive, 44px min touch targets
- 22 tests covering all 6 test requirements (T12.3.1–T12.3.6) + accessibility

### Change Log
| File | Change |
|------|--------|
| `src/components/nexus/OnboardingScreen.tsx` | Created — onboarding form with validation, wallet creation, localStorage, reload |
| `src/components/nexus/EmptyDashboard.tsx` | Created — empty state with Ativos navigation |
| `tests/unit/onboarding.test.tsx` | Created — 22 unit tests |
| `src/index.ts` | Added exports for OnboardingScreen, OnboardingScreenProps, EmptyDashboard, EmptyDashboardProps |

### Debug Log
- Pre-existing TS errors in index.ts (supabase export, formatBrl ambiguity) — not introduced by this story
- All 903 tests pass (22 new + 881 existing)
