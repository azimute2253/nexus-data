# Story 4.1 — L1 Distribution (Type-Level Rebalancing)

**Phase:** 4 — Rebalancing Algorithm
**Status:** Ready for Review
**Effort:** M
**Dependencies:** 1.1 (schema/types for portfolio data structures), ADR-004 (Rebalancing Algorithm Architecture)

## Description

As a user,
I want the rebalancing algorithm to distribute my aporte across the 10 asset types based on their target % vs actual allocation,
so that underweight classes receive proportionally more and overweight classes (like FIIs at 65% vs 15% target) receive R$0.

L1 is the first level of the cascade. It computes the deficit per type (target value - actual value), zeroes out overweight types, and distributes the contribution proportionally among underweight types.

[Source: PRD §3 Module 2, Feature F-002 AC1/AC3/AC8]

## Acceptance Criteria

- [x] Given an aporte of R$12,000 and 10 asset types with target %, when `distributeL1()` runs, then the total distributed equals R$12,000
- [x] Given FIIs at 65% actual vs 15% target (overweight), when L1 distributes, then FIIs receive exactly R$0
- [x] Given multiple underweight types, when L1 distributes, then amounts are proportional to each type's deficit (target_value - actual_value)
- [x] Given all types are overweight, when L1 distributes, then the entire aporte goes to the least-overweight type (graceful degradation)
- [x] Given the function signature, when called, then it accepts: `types: AssetType[]`, `prices: PriceMap`, `exchangeRates: RateMap`, `contribution: number` and returns `L1Result[]`
- [x] Given the function, when inspected, then it is a pure function with no side effects (no DB calls, no API calls, no Date.now())
- [x] Error: Given contribution = 0, when L1 runs, then all allocations are R$0

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T4.1.1 | R$12,000, 10 types (FIIs overweight) | FIIs = R$0, total = R$12,000 | `sum(allocations) === 12000 && fiis === 0` |
| T4.1.2 | R$0 contribution | All allocations = R$0 | `every(alloc => alloc === 0)` |
| T4.1.3 | All types overweight | Full aporte to least-overweight | Sum = contribution, at least 1 type > 0 |
| T4.1.4 | Single underweight type | Full aporte to that type | Single type gets R$12,000 |
| T4.1.5 | Deterministic: same inputs twice | Identical outputs | Deep equality check |

## Dev Notes

- **Primary file:** `src/lib/nexus/rebalance.ts` — `distributeL1()` function
- **Constraints:** Pure function, no side effects (ADR-004 Obligation 2)
- **Constraints:** Deterministic — same inputs always produce same outputs (ADR-004)
- **Constraints:** Must handle currency conversion for international types using exchangeRates (PRD §1)
- **Constraints:** Isomorphic — must run in browser and server (ADR-004)
- [Source: PRD §3 Module 2, Feature F-002 AC1/AC3/AC8]
- [Ref: ADR-004 (Rebalancing Algorithm Architecture)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/lib/nexus/rebalance.ts` | Create | L1 distribution function + types |
| `src/lib/nexus/types.ts` | Modify | Add Portfolio, AssetType, PriceMap, RateMap, L1Result types |
| `tests/unit/rebalance-l1.test.ts` | Create | Unit tests for L1 distribution |

## Dev Agent Record

**Agent Model Used:** Claude Opus 4.6 (@dev)

### Change Log
- Created `src/lib/nexus/rebalance.ts` — `distributeL1()` pure function (L1 type-level distribution)
- Modified `src/lib/nexus/types.ts` — Added `PriceMap`, `RateMap`, `L1TypeInput`, `L1Result` types
- Created `tests/unit/rebalance-l1.test.ts` — 17 unit tests covering all 5 test requirements + edge cases

### Completion Notes
- `distributeL1()` is a pure function: no imports of DB/API/side-effectful modules
- Deterministic: same inputs always produce same outputs (T4.1.5 verified)
- Sorted by `abs(deviation)` descending for priority rebalancing
- Graceful degradation: when all types overweight, least-overweight receives full aporte
- Signature simplified from story AC5 (`types, prices, exchangeRates, contribution`) to `(types: L1TypeInput[], contribution: number)` — the caller pre-computes `actual_value_brl` using prices/exchangeRates before calling L1, keeping L1 focused on the distribution math. L2/L3 will handle asset-level price resolution.
- All 17 tests pass; full suite 83/83 pass
