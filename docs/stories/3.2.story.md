# Story 3.2 — Yahoo Finance Price Adapter (International ETFs)

**Phase:** 3 — Price Engine
**Status:** Draft
**Effort:** M
**Dependencies:** 1.1 (price_cache table), 3.1 (PriceProvider interface)

## Description

As a developer,
I want a price adapter for Yahoo Finance that fetches quotes for ~20 international ETFs with circuit breaker protection,
so that ETF prices are available for portfolio valuation and rebalancing.

Yahoo Finance has no SLA — the adapter must be resilient with a circuit breaker (3 consecutive failures per ticker → 1 hour cooldown). All ~20 ETFs must be validated during Sprint 1.

[Source: PRD §3 Module 5, Feature F-007 AC2]

## Acceptance Criteria

- [ ] Given ~20 international ETF tickers (QQQM, SCHD, AVUV, etc.), when `fetchPrices()` is called, then each ticker gets an individual request to Yahoo Finance
- [ ] Given a successful response, when processed, then each result includes: `ticker`, `price` in USD, `currency: 'USD'`, `source: 'yahoo'`, `fetched_at`
- [ ] Given 3 consecutive failures for a ticker, when the circuit breaker triggers, then that ticker is skipped for 1 hour with a UI alert surfaced
- [ ] Given the circuit breaker state, when cooldown expires, then the next refresh attempt includes the previously-failed ticker
- [ ] Given a ticker that fails persistently, when manual fallback is needed, then the asset can have `price_source` set to `'manual'` via the UI
- [ ] Error: Given Yahoo Finance returns an error or timeout, when detected, then the error is logged to `price_fetch_log` with provider, ticker, status, and error message
- [ ] Error: Given Yahoo Finance is completely down, when all tickers fail, then existing cached prices remain in `price_cache` with original `fetched_at` timestamps

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T3.2.1 | Valid ETF ticker "QQQM" | Price returned in USD | `price > 0 && currency === 'USD'` |
| T3.2.2 | 3 consecutive failures for "AVUV" | Circuit breaker open | Ticker skipped on 4th attempt |
| T3.2.3 | Circuit breaker open + 1h elapsed | Circuit breaker half-open | Ticker retried |
| T3.2.4 | All ~20 ETF tickers | Valid prices or documented failures | Validation report generated |

## Dev Notes

- **Primary file:** `supabase/functions/shared/providers/yahoo-provider.ts`
- **Constraints:** Must implement `PriceProvider` interface (ADR-003 Obligation 1)
- **Constraints:** Circuit breaker: 3 failures → 1h cooldown (ADR-003 Obligation 5)
- **Constraints:** ALL ~20 ETFs must be validated before end of Sprint 1 (ADR-003 Obligation 2)
- **Constraints:** Error logging to `price_fetch_log` (ADR-003 Obligation 3)
- [Source: PRD §3 Module 5, Feature F-007 AC2]
- [Ref: ADR-003 (Yahoo Finance mitigation plan), ADR-005 (cache strategy)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `supabase/functions/shared/price-engine/aggregator.ts` | Create | Price aggregator combining brapi + exchange rate → BRL |
| `supabase/functions/shared/price-engine/aggregator.test.ts` | Create | Unit tests for price aggregator (12 tests) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Change Log
- 2026-03-22: Created `aggregator.ts` — `aggregatePrice()` and `aggregatePrices()` functions
- 2026-03-22: Created `aggregator.test.ts` — 12 unit tests covering all acceptance criteria
- 2026-03-22: All 161 tests pass (9 test files), `tsc --noEmit` clean

### Completion Notes
- Story implemented as Price Aggregator per user task (overrides original Yahoo Finance adapter scope)
- `aggregatePrice(request, options)` — single ticker with BRL normalization
- `aggregatePrices(requests, options)` — batch mode, groups by currency to minimize exchange rate fetches
- BRL tickers skip exchange rate call entirely (optimization)
- USD tickers fetch exchange rate and multiply by USD/BRL
- Error propagation preserves original brapi/exchange error messages
- Uses adapters from Story 3.1 (BrapiProvider) and Story 3.3 (ExchangeRateProvider)
