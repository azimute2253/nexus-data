# Story 6.4 — Asset Flags (is_active + manual_override)

**Phase:** 6 — Asset Management
**Status:** Draft
**Effort:** S
**Dependencies:** 6.3 (asset CRUD must exist)

## Description

As a user,
I want to toggle `is_active` and `manual_override` flags on any asset across all 10 types,
so that I can exclude assets from rebalancing without deleting them — replacing the spreadsheet's "Grupo 0" hack and the FII-only "Vou aportar?" flag.

This directly addresses PRD Goal G6 and Creative problem #6 (override centralizado).

[Source: PRD §3 Module 3, Feature F-005 AC3/AC4]

## Acceptance Criteria

- [ ] Given any asset in any of the 10 types, when I toggle `is_active` to false, then the asset is excluded from rebalancing without being deleted from the database
- [ ] Given any asset, when I toggle `manual_override` to true, then the asset is temporarily excluded from rebalancing (equivalent to "Vou aportar? Nao")
- [ ] Given an inactive asset, when the asset list is displayed, then the asset is visually dimmed/grayed with an "Inactive" badge
- [ ] Given an overridden asset, when the asset list is displayed, then the asset shows a "Pausado" badge
- [ ] Given the flags are toggled, when the rebalance algorithm runs, then excluded assets receive 0 shares and their group allocation is redistributed among remaining active assets
- [ ] Given all 10 asset types, when inspected, then both flags are available for every type (not just FIIs) — covers PRD Goal G6
- [ ] Error: Given a group where all assets have `is_active = false` or `manual_override = true`, when rebalancing, then the group's allocation is returned as unallocated remainder

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T6.4.1 | Set VALE3 is_active=false | Asset dimmed in list | Visual indicator present |
| T6.4.2 | Rebalance with VALE3 inactive | VALE3 gets 0 shares | shares_to_buy = 0 |
| T6.4.3 | Set XPLG11 manual_override=true | "Pausado" badge shown | Badge in DOM |
| T6.4.4 | Check all 10 types for flag support | Both flags available | DB columns exist, UI toggles work |
| T6.4.5 | All assets in group inactive | Remainder returned | Unallocated amount in result |

## Dev Notes

- **Primary file:** `src/components/nexus/AssetTable.tsx` (add toggle controls)
- **Constraints:** Both flags available for ALL 10 types — not just FIIs (PRD G6, Success Metric)
- **Constraints:** Rebalance algorithm respects both flags (Story 4.3)
- [Source: PRD §3 Module 3, Feature F-005 AC3/AC4, Goal G6]
- [Ref: ADR-004 (excluded assets in L3 distribution)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `supabase/migrations/014_asset_flags.sql` | Create | Migration adding bought/sold columns + exclusivity constraint |
| `src/lib/nexus/types.ts` | Modify | Add bought/sold fields to Asset interface |
| `src/lib/nexus/assets.ts` | Modify | Add bought/sold validation in createAsset and updateAsset |
| `tests/unit/assets.test.ts` | Modify | Add 7 tests for bought/sold flag operations |

## Dev Agent Record

### Change Log
- Added migration 014 with `bought BOOLEAN DEFAULT false`, `sold BOOLEAN DEFAULT false`, and CHECK constraint `chk_bought_sold_exclusive`
- Added `bought: boolean` and `sold: boolean` to `Asset` interface in types.ts
- Added client-side validation in `createAsset()` and `updateAsset()`: rejects when both bought and sold are true
- Added 7 new tests (T6.4.1–T6.4.7) covering: set bought, set sold, reject both true, reset both, bought+sold=false combo, createAsset validation, getAssets returns flags
- Updated all test fixtures to include bought/sold fields

### Completion Notes
- Schema already had `is_active` and `manual_override` columns from migration 007 — no changes needed for those
- DB constraint `chk_bought_sold_exclusive` enforces exclusivity at database level as safety net
- All 578 tests pass, TypeScript clean
