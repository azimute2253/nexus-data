# Story 6.3 — CRUD Individual Assets

**Phase:** 6 — Asset Management
**Status:** Ready for Review
**Effort:** M
**Dependencies:** 6.2 (groups must exist), 3.4 (prices for display), 1.1 (schema)

## Description

As a user,
I want to add, edit, and view individual assets with their ticker, sector, quantity, group, and price source,
so that I can manage all 131+ positions in one place instead of editing 3 separate spreadsheet tabs.

[Source: PRD §3 Module 3, Feature F-005 AC1/AC2/AC5/AC6]

## Acceptance Criteria

- [ ] Given a selected group, when assets are loaded, then all assets in that group are listed with: ticker, sector, quantity, current price, value (BRL)
- [ ] Given the add form, when I fill in ticker, sector, quantity, and select a price source (brapi/yahoo/manual/crypto), then a new asset is created in the database
- [ ] Given an existing asset, when I edit any field (ticker, sector, quantity, group, price_source), then the change is persisted
- [ ] Given 131+ assets, when the list renders, then there is no perceptible performance degradation (< 200ms render)
- [ ] Given the price source selector, when displayed, then options are: brapi, yahoo, manual, crypto
- [ ] Given the asset list, when displayed, then each row shows: ticker, sector, quantity, price, value (BRL), is_active status, manual_override status
- [ ] Error: Given a duplicate ticker in the same group, when creating, then a validation error is shown
- [ ] Error: Given an empty ticker field, when saving, then validation prevents the save

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T6.3.1 | Select group in Acoes BR | Assets listed with prices | Assets displayed with all fields |
| T6.3.2 | Add MGLU3, Varejo, 0, brapi | Asset created | DB row exists with correct data |
| T6.3.3 | Edit VALE3 quantity from 10 to 12 | Updated | DB quantity = 12 |
| T6.3.4 | 131+ assets render | < 200ms | Performance measurement |
| T6.3.5 | Duplicate ticker "VALE3" | Validation error: "Ticker already exists in this group" | Error message shown, save prevented |
| T6.3.6 | Save asset with empty ticker field | Validation error: "Ticker is required" | Error message shown, save prevented |

## Dev Notes

- **Primary file:** `src/components/nexus/AssetTable.tsx` and `src/components/nexus/AssetForm.tsx`
- **Constraints:** Assets page at L3 route: `/dashboard/assets/[type]/[group]` (ADR-006)
- **Constraints:** Sortable, filterable list (ADR-006 — AssetTable component)
- **Constraints:** Hydration: `client:load` (ADR-006 Hydration Strategy)
- [Source: PRD §3 Module 3, Feature F-005 AC1/AC2/AC5/AC6]
- [Ref: ADR-002 (assets table), ADR-006 (AssetTable + AssetForm)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/lib/nexus/assets.ts` | Create | CRUD operations for assets table |
| `tests/unit/assets.test.ts` | Create | Unit tests for assets CRUD (31 tests) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### Completion Notes
- Implemented CRUD operations following established patterns from Stories 6.1/6.2
- Files placed in `src/lib/nexus/` (project convention), not `src/lib/crud/` as originally suggested
- Asset types in DB: `Asset`, `AssetInsert`, `AssetUpdate` already defined in `types.ts`
- Validations: ticker required, quantity >= 0, group_id required, price_source enum check
- Unique ticker constraint `(ticker, user_id)` enforced at DB level (migration 010)
- Story AC references `target_weight` but schema uses `target_pct` on types/groups — assets don't have individual target weights (allocation is score-driven via L3 rebalance)
- All 31 unit tests passing, 519 total tests passing (zero regressions)
- Pre-existing TSX typecheck errors in AllocationTable.tsx and RebalanceCalculator.tsx (not related to this story)

### Change Log
| Date | Change |
|------|--------|
| 2026-03-22 | Created `src/lib/nexus/assets.ts` — CRUD functions: getAssets, getAsset, createAsset, updateAsset, deleteAsset |
| 2026-03-22 | Created `tests/unit/assets.test.ts` — 31 unit tests covering all CRUD ops and validations |
