# Story 11.3 — Wallet Data Access Layer (CRUD + Isolation)

**Epic:** 11 — Multi-Wallet Foundation
**Status:** Ready for Review
**Effort:** M (3 points)
**Dependencies:** 11.2 (wallet_id FK must exist on all tables)
**PRD Feature:** F-022, F-024

## Description

As a developer,
I want a data access layer for wallets (CRUD operations) with wallet-scoped data functions,
so that all data queries are automatically filtered by the active wallet and the application correctly isolates data between wallets.

This includes updating all existing data functions (asset types, groups, assets, scores, contributions) to accept and filter by `wallet_id`.

[Source: PRD-v2 F-022, F-024; ADR-014 (Data Isolation — RLS by user_id + app-layer filter by wallet_id)]

## Acceptance Criteria

- [x] Given the wallet client module, when inspected, then it exports: `getWallets(userId)`, `getWallet(id)`, `createWallet(name)`, `updateWallet(id, name)`, `deleteWallet(id)`
- [x] Given `createWallet(name)`, when called with a valid name, then a new wallet is created and returned with `id`, `user_id`, `name`, `created_at`
- [x] Given `deleteWallet(id)`, when called, then the wallet and ALL associated data (6 tables) are cascade-deleted
- [x] Given all existing data functions, when inspected, then they accept `wallet_id` as a required parameter and filter queries by it
- [x] Given wallet A with 3 asset types and wallet B with 2 asset types, when `getAssetTypes(walletId_A)` is called, then only wallet A's 3 types are returned
- [x] Given a wallet name update, when `updateWallet(id, newName)` is called, then the name is updated and the response confirms the change
- [x] Given the wallet name validation, when a name < 1 char or > 50 chars is provided, then the function throws a validation error

## Test Requirements

| Test | Input | Expected | Validation |
|------|-------|----------|------------|
| T11.3.1 | createWallet("Test") | Wallet created with name "Test" | Returned object has id + name |
| T11.3.2 | getWallets() for user with 2 wallets | Array of 2 wallets | Length check |
| T11.3.3 | deleteWallet(id) | Wallet + all cascade data removed | 0 rows in all 6 child tables |
| T11.3.4 | getAssetTypes(walletA) vs getAssetTypes(walletB) | Different results | Data isolation confirmed |
| T11.3.5 | createWallet("") | Validation error | Error thrown |
| T11.3.6 | updateWallet(id, "New Name") | Name updated | GET returns new name |

## Dev Notes

- **Primary files:**
  - `src/lib/nexus/wallets.ts` — Wallet CRUD client
  - All existing data files in `src/lib/nexus/` — Add wallet_id parameter
- **Constraints:** RLS enforces user_id isolation at DB level; wallet_id isolation is app-layer (ADR-014)
- **Constraints:** TypeScript must enforce wallet_id as required parameter (compile-time safety)
- **Constraints:** `isWalletOwner(walletId)` helper for ownership verification before mutations
- [Source: PRD-v2 F-022, F-024; Creative V2 §Arquitetura de Dados]
- [Ref: ADR-014 (data isolation strategy), ADR-010 (schema)]

## File List

| File | Action | Description |
|------|--------|-------------|
| `src/lib/nexus/wallets.ts` | Create | Wallet CRUD functions + isWalletOwner helper |
| `src/lib/nexus/contributions.ts` | Create | Contributions CRUD with wallet_id |
| `src/lib/nexus/asset-types.ts` | Modify | Add wallet_id parameter to getAssetTypes() |
| `src/lib/nexus/groups.ts` | Modify | Add wallet_id parameter to getGroups() |
| `src/lib/nexus/assets.ts` | Modify | Add wallet_id parameter to getAssets() |
| `src/lib/nexus/questionnaires.ts` | Modify | Add wallet_id parameter to getQuestionnaires() |
| `src/lib/nexus/asset-scores.ts` | Modify | Add wallet_id parameter to getScoresByQuestionnaire() |
| `src/index.ts` | Modify | Export wallets + contributions modules |
| `tests/unit/wallets.test.ts` | Create | Wallet CRUD + isolation tests (T11.3.1-T11.3.6) |
| `tests/unit/asset-types.test.ts` | Modify | Update getAssetTypes calls with wallet_id |
| `tests/unit/groups.test.ts` | Modify | Update getGroups calls with wallet_id |
| `tests/unit/assets.test.ts` | Modify | Update getAssets calls with wallet_id |
| `tests/unit/questionnaires.test.ts` | Modify | Update getQuestionnaires calls with wallet_id |
| `tests/unit/asset-scores.test.ts` | Modify | Update getScoresByQuestionnaire calls with wallet_id |

## Dev Agent Record

**Agent Model Used:** claude-opus-4-6

### Change Log

| Date | Change |
|------|--------|
| 2026-03-24 | Created `wallets.ts` with CRUD: getWallets, getWallet, createWallet, updateWallet, deleteWallet, isWalletOwner |
| 2026-03-24 | Created `contributions.ts` with CRUD: getContributions, getContribution, createContribution, updateContribution, deleteContribution |
| 2026-03-24 | Updated 5 data modules to accept wallet_id as required filter parameter on list queries |
| 2026-03-24 | Updated 5 existing test files to pass wallet_id to modified function signatures |
| 2026-03-24 | Created `wallets.test.ts` covering T11.3.1-T11.3.6 + cross-wallet isolation |
| 2026-03-24 | Exported wallets + contributions modules from index.ts |

### Debug Log

- No issues encountered during implementation

### Completion Notes

- All 818 tests pass (42 test files)
- TypeScript check passes (only pre-existing unrelated errors in `index.ts`)
- wallet_id is required parameter on all list queries (compile-time safety)
- `isWalletOwner(walletId)` relies on RLS — if Supabase returns null, wallet is not owned
- Cascade delete relies on DB FK CASCADE constraints (set up in Story 11.2)
- `contributions.ts` was created new since the file didn't exist yet (story listed it as "Modify" but no prior file existed)
- `asset-groups.ts` in story is actually `groups.ts` in codebase; `scores.ts` is actually `asset-scores.ts`

## QA Results

**Reviewed by:** Quinn (@qa) — Claude Opus 4.6
**Date:** 2026-03-24
**Gate Decision:** PASS

### Acceptance Criteria Verdicts

| # | Criteria | Verdict |
|---|----------|---------|
| AC-1 | Wallet CRUD exports match spec | PASS |
| AC-2 | createWallet returns id, user_id, name, created_at | PASS |
| AC-3 | deleteWallet cascade-deletes 6 tables (DB FK) | PASS |
| AC-4 | All data functions accept wallet_id as required | PASS |
| AC-5 | Cross-wallet isolation (3 vs 2 types) | PASS |
| AC-6 | updateWallet updates name | PASS |
| AC-7 | Name validation (< 1 or > 50 chars) | PASS |

### Test Results

- **818/818 tests pass** (42 test files)
- **T11.3.1-T11.3.6** all pass + additional edge cases (20 tests in wallets.test.ts)
- TypeScript: 2 pre-existing errors in index.ts (unrelated to this story)

### Recommendations (non-blocking)

1. Fix pre-existing TS errors in `src/index.ts` (separate maintenance story)
2. Cascade delete (T11.3.3) relies on DB FK — integration test would strengthen coverage
